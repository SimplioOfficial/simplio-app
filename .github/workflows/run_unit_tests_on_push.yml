name: Run unit tests

on:
  push:
    branches:
      - '*'

jobs:
  run-tests:
    uses: SimplioOfficial/simplio-app/.github/workflows/_test_application.yml@10_ci_cd_setup
    with:
      e2e: false
