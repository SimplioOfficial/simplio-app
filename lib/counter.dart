// redundant class for testing unit tests.
// Feel free to remove it in the future (together with the unit tests)
class Counter {
  int value = 0;

  void increment() => value++;

  void decrement() => value--;
}
